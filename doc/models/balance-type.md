
# Balance Type

Discriminator on a balance object. `COMPANY` selects a `CompanyBalanceObject`; `USER` selects a `UserBalanceObject`. See [Balance Type](page:resources/balances) for the discriminator semantics.

## Enumeration

`BalanceType`

## Fields

| Name |
|  --- |
| `COMPANY` |

## Example

```ruby
balance_type = BalanceType::COMPANY
```

