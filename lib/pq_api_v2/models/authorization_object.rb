# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

require 'date'
module PqApiV2
  # Authorization — a pending card transaction. A merchant has authorized a
  # charge against one of the program's prepaid cards but the transaction has
  # not yet settled; funds are reserved against the card until settlement, at
  # which point a [Receipt](page:resources/receipts) replaces this
  # authorization. Authorizations are a top-level transactional resource peer to
  # [Transfers](page:resources/transfers). See
  # [Authorizations](page:resources/authorizations).
  class AuthorizationObject < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Kind of card-transaction authorization (e.g., purchase, ATM withdrawal,
    # refund). See [Card Status
    # Types](page:additional-api-information/card-status-types) and the
    # [Authorizations](page:resources/authorizations) resource for the value
    # semantics.
    # @return [String]
    attr_accessor :type

    # Date and time when the resource was created. ISO 8601 format.
    # @return [DateTime]
    attr_accessor :created_at

    # Direction of a [Receipt](page:resources/receipts) amount relative to the
    # account that owns it — `CREDIT` (funds flowed into the account), `DEBIT`
    # (funds flowed out), or `UNDEFINED`. Used to interpret receipt amounts on a
    # per-account basis — the same money movement produces a `CREDIT` receipt on
    # one side and a `DEBIT` receipt on the other.
    # @return [String]
    attr_accessor :sign

    # Token of the prepaid card the merchant authorized against (`dest-`
    # prefix).
    # @return [String]
    attr_accessor :source_token

    # Allocated money to be sent in the transaction, represented as a string to
    # preserve decimal precision. On transfer-quote responses this value is
    # **always non-negative**, regardless of transfer direction. ([learn
    # more](page:concepts/monetary-precision#quote-responses-are-always-non-nega
    # tive))
    # @return [String]
    attr_accessor :amount

    # ISO 4217 three-letter currency code (e.g. `USD`, `EUR`, `GBP`). See
    # [Currency Codes](page:additional-api-information/currency-codes) for the
    # supported list.
    # @return [String]
    attr_accessor :currency

    # Status of the underlying receipt (transaction) that was created when this
    # quote was accepted. This field is only populated after the quote has been
    # accepted and a receipt exists. When the quote is still pending or has been
    # cancelled, this field is null. See [Receipt Status
    # Types](page:additional-api-information/receipt-status-types) for the full
    # list of possible values.
    # @return [String]
    attr_accessor :status

    # Localized description of the authorization, in the language specified by
    # the request's Accept-Language header. Falls back to en-US if the requested
    # language is unavailable.
    # @return [String]
    attr_accessor :description

    # Date that the auth was created
    # @return [DateTime]
    attr_accessor :auth_date

    # Merchant or network reference for the authorization, when available.
    # Useful for reconciling an authorization with the corresponding entry in
    # the cardholder's merchant statement.
    # @return [String]
    attr_accessor :reference

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['type'] = 'type'
      @_hash['created_at'] = 'createdAt'
      @_hash['sign'] = 'sign'
      @_hash['source_token'] = 'sourceToken'
      @_hash['amount'] = 'amount'
      @_hash['currency'] = 'currency'
      @_hash['status'] = 'status'
      @_hash['description'] = 'description'
      @_hash['auth_date'] = 'authDate'
      @_hash['reference'] = 'reference'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      []
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(type:, created_at:, sign:, source_token:, amount:,
                   currency: 'USD', status:, description:, auth_date:,
                   reference:, additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @type = type
      @created_at = created_at
      @sign = sign
      @source_token = source_token
      @amount = amount
      @currency = currency
      @status = status
      @description = description
      @auth_date = auth_date
      @reference = reference
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      type = hash.key?('type') ? hash['type'] : nil
      created_at = if hash.key?('createdAt')
                     (DateTimeHelper.from_rfc3339(hash['createdAt']) if hash['createdAt'])
                   end
      sign = hash.key?('sign') ? hash['sign'] : nil
      source_token = hash.key?('sourceToken') ? hash['sourceToken'] : nil
      amount = hash.key?('amount') ? hash['amount'] : nil
      currency = hash['currency'] ||= 'USD'
      status = hash.key?('status') ? hash['status'] : nil
      description = hash.key?('description') ? hash['description'] : nil
      auth_date = if hash.key?('authDate')
                    (DateTimeHelper.from_rfc3339(hash['authDate']) if hash['authDate'])
                  end
      reference = hash.key?('reference') ? hash['reference'] : nil

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      AuthorizationObject.new(type: type,
                              created_at: created_at,
                              sign: sign,
                              source_token: source_token,
                              amount: amount,
                              currency: currency,
                              status: status,
                              description: description,
                              auth_date: auth_date,
                              reference: reference,
                              additional_properties: additional_properties)
    end

    def to_custom_created_at
      DateTimeHelper.to_rfc3339(created_at)
    end

    def to_custom_auth_date
      DateTimeHelper.to_rfc3339(auth_date)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} type: #{@type}, created_at: #{@created_at}, sign: #{@sign}, source_token:"\
      " #{@source_token}, amount: #{@amount}, currency: #{@currency}, status: #{@status},"\
      " description: #{@description}, auth_date: #{@auth_date}, reference: #{@reference},"\
      " additional_properties: #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} type: #{@type.inspect}, created_at: #{@created_at.inspect}, sign:"\
      " #{@sign.inspect}, source_token: #{@source_token.inspect}, amount: #{@amount.inspect},"\
      " currency: #{@currency.inspect}, status: #{@status.inspect}, description:"\
      " #{@description.inspect}, auth_date: #{@auth_date.inspect}, reference:"\
      " #{@reference.inspect}, additional_properties: #{@additional_properties}>"
    end
  end
end
