# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module PqApiV2
  # Exchange rate. On transfer-quote responses the `sourceAmount` and
  # `destinationAmount` fields are **always non-negative** (and their formatted
  # counterparts are always sign-free); `rate` itself follows mathematical
  # convention and is unsigned by construction. ([learn
  # more](page:concepts/monetary-precision#quote-responses-are-always-non-negati
  # ve))
  class FxRate < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Allocated money to be sent in the transaction, represented as a string to
    # preserve decimal precision. On transfer-quote responses this value is
    # **always non-negative**, regardless of transfer direction — direction is
    # conveyed by `transferType`, not by sign. ([learn
    # more](page:concepts/monetary-precision#quote-responses-are-always-non-nega
    # tive))
    # @return [String]
    attr_accessor :destination_amount

    # ISO 4217 three-letter currency code (e.g. `USD`, `EUR`, `GBP`). See
    # [Currency Codes](page:additional-api-information/currency-codes) for the
    # supported list.
    # @return [String]
    attr_accessor :destination_currency

    # Combination of the amount and currency type. On transfer-quote responses
    # this string is **always sign-free** (never contains `-`, `(`, or `)`) — it
    # can be rendered directly without further sign-stripping. ([learn
    # more](page:concepts/monetary-precision#quote-responses-are-always-non-nega
    # tive))
    # @return [String]
    attr_accessor :destination_formatted_amount

    # Exchange rate, represented as a string to preserve decimal precision
    # ([learn more](page:concepts/monetary-precision)).
    # @return [String]
    attr_accessor :rate

    # Allocated money to be sent in the transaction, represented as a string to
    # preserve decimal precision. On transfer-quote responses this value is
    # **always non-negative**, regardless of transfer direction. ([learn
    # more](page:concepts/monetary-precision#quote-responses-are-always-non-nega
    # tive))
    # @return [String]
    attr_accessor :source_amount

    # ISO 4217 three-letter currency code (e.g. `USD`, `EUR`, `GBP`). See
    # [Currency Codes](page:additional-api-information/currency-codes) for the
    # supported list.
    # @return [String]
    attr_accessor :source_currency

    # Combination of the amount and currency type. On transfer-quote responses
    # this string is **always sign-free** (never contains `-`, `(`, or `)`) — it
    # can be rendered directly without further sign-stripping. ([learn
    # more](page:concepts/monetary-precision#quote-responses-are-always-non-nega
    # tive))
    # @return [String]
    attr_accessor :source_formatted_amount

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['destination_amount'] = 'destinationAmount'
      @_hash['destination_currency'] = 'destinationCurrency'
      @_hash['destination_formatted_amount'] = 'destinationFormattedAmount'
      @_hash['rate'] = 'rate'
      @_hash['source_amount'] = 'sourceAmount'
      @_hash['source_currency'] = 'sourceCurrency'
      @_hash['source_formatted_amount'] = 'sourceFormattedAmount'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        destination_amount
        destination_currency
        destination_formatted_amount
        rate
        source_amount
        source_currency
        source_formatted_amount
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(destination_amount: SKIP, destination_currency: 'USD',
                   destination_formatted_amount: '$0.05 USD', rate: SKIP,
                   source_amount: SKIP, source_currency: 'USD',
                   source_formatted_amount: '$0.05 USD',
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @destination_amount = destination_amount unless destination_amount == SKIP
      @destination_currency = destination_currency unless destination_currency == SKIP
      unless destination_formatted_amount == SKIP
        @destination_formatted_amount =
          destination_formatted_amount
      end
      @rate = rate unless rate == SKIP
      @source_amount = source_amount unless source_amount == SKIP
      @source_currency = source_currency unless source_currency == SKIP
      @source_formatted_amount = source_formatted_amount unless source_formatted_amount == SKIP
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      destination_amount =
        hash.key?('destinationAmount') ? hash['destinationAmount'] : SKIP
      destination_currency = hash['destinationCurrency'] ||= 'USD'
      destination_formatted_amount =
        hash['destinationFormattedAmount'] ||= '$0.05 USD'
      rate = hash.key?('rate') ? hash['rate'] : SKIP
      source_amount = hash.key?('sourceAmount') ? hash['sourceAmount'] : SKIP
      source_currency = hash['sourceCurrency'] ||= 'USD'
      source_formatted_amount = hash['sourceFormattedAmount'] ||= '$0.05 USD'

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      FxRate.new(destination_amount: destination_amount,
                 destination_currency: destination_currency,
                 destination_formatted_amount: destination_formatted_amount,
                 rate: rate,
                 source_amount: source_amount,
                 source_currency: source_currency,
                 source_formatted_amount: source_formatted_amount,
                 additional_properties: additional_properties)
    end

    # Validates an instance of the object from a given value.
    # @param [FxRate | Hash] The value against the validation is performed.
    def self.validate(value)
      return true if value.instance_of? self

      return false unless value.instance_of? Hash

      true
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} destination_amount: #{@destination_amount}, destination_currency:"\
      " #{@destination_currency}, destination_formatted_amount: #{@destination_formatted_amount},"\
      " rate: #{@rate}, source_amount: #{@source_amount}, source_currency: #{@source_currency},"\
      " source_formatted_amount: #{@source_formatted_amount}, additional_properties:"\
      " #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} destination_amount: #{@destination_amount.inspect}, destination_currency:"\
      " #{@destination_currency.inspect}, destination_formatted_amount:"\
      " #{@destination_formatted_amount.inspect}, rate: #{@rate.inspect}, source_amount:"\
      " #{@source_amount.inspect}, source_currency: #{@source_currency.inspect},"\
      " source_formatted_amount: #{@source_formatted_amount.inspect}, additional_properties:"\
      " #{@additional_properties}>"
    end
  end
end
