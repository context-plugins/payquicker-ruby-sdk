# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

require 'date'
module PqApiV2
  # Electronic-wallet [Instrument](page:resources/instruments) — a payout
  # destination representing a payee's account on a third-party wallet provider
  # (e.g., PayPal, Venmo, Airtel Money). Carries the provider type, country,
  # currency, the provider-specific identifying fields (account ID, phone
  # number, etc.), and a verification status. The unified Instruments resource
  # discriminates electronic wallets vs bank accounts via `instrumentType:
  # EWALLET`.
  class EwalletBase < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Token identifying the destination of funds — a bank-account or
    # electronic-wallet [Instrument](page:resources/instruments), or a [Prepaid
    # Card](page:resources/prepaid-cards). Prefix `dest-`.
    # @return [String]
    attr_accessor :token

    # Discriminator value identifying this as an electronic wallet instrument.
    # @return [InstrumentType1]
    attr_accessor :instrument_type

    # Provider type of an electronic-wallet
    # [Instrument](page:resources/instruments) — identifies which third-party
    # wallet network the destination uses (e.g., `PAYPAL`, `VENMO`,
    # `AIRTEL_MONEY`). Available providers depend on the payee's country and the
    # program's configuration; discover them via `POST
    # /instruments/requirements/search`. See [Transfer Method
    # Types](page:additional-api-information/transfer-method-types) for the
    # rails reference.
    # @return [String]
    attr_accessor :type

    # ISO 3166-1 alpha-2 country code (two-letter, e.g., `US`, `GB`, `DE`). Used
    # throughout the PayQuicker API in place of the full country name — for
    # residential / mailing addresses on [Users](page:resources/users), for the
    # registered country of [bank-account](page:resources/instruments) and
    # [electronic-wallet](page:resources/instruments) instruments, and to gate
    # which destinations / methods are available to a payee. See [Country
    # Codes](page:additional-api-information/country-codes) for the supported
    # list.
    # @return [String]
    attr_accessor :country

    # ISO 4217 three-letter currency code (e.g. `USD`, `EUR`, `GBP`). See
    # [Currency Codes](page:additional-api-information/currency-codes) for the
    # supported list.
    # @return [String]
    attr_accessor :currency

    # Date and time when the resource was created. ISO 8601 format.
    # @return [DateTime]
    attr_accessor :created_at

    # Array of electronic wallet fields and values
    # @return [Array[EwalletField]]
    attr_accessor :fields

    # Current verification status of an electronic-wallet
    # [Instrument](page:resources/instruments). The status progresses from
    # creation through provider verification — values mirror the bank-account
    # status set (e.g., the wallet is awaiting validation, has been approved, or
    # is under compliance hold).
    # @return [String]
    attr_accessor :status

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['token'] = 'token'
      @_hash['instrument_type'] = 'instrumentType'
      @_hash['type'] = 'type'
      @_hash['country'] = 'country'
      @_hash['currency'] = 'currency'
      @_hash['created_at'] = 'createdAt'
      @_hash['fields'] = 'fields'
      @_hash['status'] = 'status'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        fields
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(token:, instrument_type: InstrumentType1::EWALLET, type:,
                   country:, currency:, created_at:, status:, fields: SKIP,
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @token = token
      @instrument_type = instrument_type
      @type = type
      @country = country
      @currency = currency
      @created_at = created_at
      @fields = fields unless fields == SKIP
      @status = status
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      token = hash.key?('token') ? hash['token'] : nil
      instrument_type = hash['instrumentType'] ||= InstrumentType1::EWALLET
      type = hash.key?('type') ? hash['type'] : nil
      country = hash.key?('country') ? hash['country'] : nil
      currency = hash.key?('currency') ? hash['currency'] : nil
      created_at = if hash.key?('createdAt')
                     (DateTimeHelper.from_rfc3339(hash['createdAt']) if hash['createdAt'])
                   end
      status = hash.key?('status') ? hash['status'] : nil
      # Parameter is an array, so we need to iterate through it
      fields = nil
      unless hash['fields'].nil?
        fields = []
        hash['fields'].each do |structure|
          fields << (EwalletField.from_hash(structure) if structure)
        end
      end

      fields = SKIP unless hash.key?('fields')

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      EwalletBase.new(token: token,
                      instrument_type: instrument_type,
                      type: type,
                      country: country,
                      currency: currency,
                      created_at: created_at,
                      status: status,
                      fields: fields,
                      additional_properties: additional_properties)
    end

    def to_custom_created_at
      DateTimeHelper.to_rfc3339(created_at)
    end

    # Validates an instance of the object from a given value.
    # @param [EwalletBase | Hash] The value against the validation is performed.
    def self.validate(value)
      if value.instance_of? self
        return (
          APIHelper.valid_type?(value.token,
                                ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.instrument_type,
                                  ->(val) { InstrumentType1.validate(val) }) and
            APIHelper.valid_type?(value.type,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.country,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.currency,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.created_at,
                                  ->(val) { val.instance_of? DateTime }) and
            APIHelper.valid_type?(value.status,
                                  ->(val) { val.instance_of? String })
        )
      end

      return false unless value.instance_of? Hash

      (
        APIHelper.valid_type?(value['token'],
                              ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['instrumentType'],
                                ->(val) { InstrumentType1.validate(val) }) and
          APIHelper.valid_type?(value['type'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['country'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['currency'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['createdAt'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['status'],
                                ->(val) { val.instance_of? String })
      )
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token}, instrument_type: #{@instrument_type}, type: #{@type},"\
      " country: #{@country}, currency: #{@currency}, created_at: #{@created_at}, fields:"\
      " #{@fields}, status: #{@status}, additional_properties: #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token.inspect}, instrument_type: #{@instrument_type.inspect},"\
      " type: #{@type.inspect}, country: #{@country.inspect}, currency: #{@currency.inspect},"\
      " created_at: #{@created_at.inspect}, fields: #{@fields.inspect}, status:"\
      " #{@status.inspect}, additional_properties: #{@additional_properties}>"
    end
  end
end
