# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

require 'date'
module PqApiV2
  # Invitation job — a CSV-uploaded batch that produces one invitation per row.
  # Carries the file metadata (filename, total / valid / invalid record counts)
  # and a status that tracks the job from `CREATED` through `COMPLETED`. The
  # individual invitations produced by the job are searchable via [`POST
  # /invitations/search`]($e/Invitations/Search%20Invitations) with a job-token
  # filter. Token prefix `jobs-`. **Invitation Jobs are a Hosted Portal concept
  # only**. See [Invitation Jobs](page:resources/invitation-jobs).
  class InvitationJobBase < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Token representing the resource. Format is `<prefix>-<uuid>` where the
    # prefix indicates the resource type. See
    # [Tokens](page:concepts/working-with-tokens).
    # @return [String]
    attr_accessor :token

    # Reference ID in the PayQuicker Hosted Portal, if applicable.
    # @return [String]
    attr_accessor :portal_id

    # The name given to a computer file in order to distinguish it from other
    # files
    # @return [String]
    attr_accessor :filename

    # Date and time when the resource was created. ISO 8601 format.
    # @return [DateTime]
    attr_accessor :created_at

    # Total number of records in the job file
    # @return [Integer]
    attr_accessor :total_count

    # Number of valid/processable records
    # @return [Integer]
    attr_accessor :valid_count

    # Number of invalid/unprocessable records
    # @return [Integer]
    attr_accessor :invalid_count

    # Kind of batch job — `PAYMENTS` for [Payment
    # Jobs](page:resources/payment-jobs) (CSV-driven batches that produce
    # payment quotes) or `INVITATIONS` for [Invitation
    # Jobs](page:resources/invitation-jobs) (CSV-driven batches that produce
    # invitations). Determines the shape of the produced items.
    # @return [String]
    attr_accessor :type

    # Lifecycle status of a [Payment Job](page:resources/payment-jobs) or
    # [Invitation Job](page:resources/invitation-jobs) — values include
    # `CREATED`, `IN_PROGRESS`, `COMPLETED`, `FAILED`, and `CANCELLED`.
    # Subscribe to the job's `*JOBS.UPDATED` / `*JOBS.COMPLETED` /
    # `*JOBS.FAILED` webhook namespaces for status change notifications.
    # @return [String]
    attr_accessor :status

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['token'] = 'token'
      @_hash['portal_id'] = 'portalId'
      @_hash['filename'] = 'filename'
      @_hash['created_at'] = 'createdAt'
      @_hash['total_count'] = 'totalCount'
      @_hash['valid_count'] = 'validCount'
      @_hash['invalid_count'] = 'invalidCount'
      @_hash['type'] = 'type'
      @_hash['status'] = 'status'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      []
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(token:, portal_id:, filename:, created_at:, total_count:,
                   valid_count:, invalid_count:, type:, status:,
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @token = token
      @portal_id = portal_id
      @filename = filename
      @created_at = created_at
      @total_count = total_count
      @valid_count = valid_count
      @invalid_count = invalid_count
      @type = type
      @status = status
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      token = hash.key?('token') ? hash['token'] : nil
      portal_id = hash.key?('portalId') ? hash['portalId'] : nil
      filename = hash.key?('filename') ? hash['filename'] : nil
      created_at = if hash.key?('createdAt')
                     (DateTimeHelper.from_rfc3339(hash['createdAt']) if hash['createdAt'])
                   end
      total_count = hash.key?('totalCount') ? hash['totalCount'] : nil
      valid_count = hash.key?('validCount') ? hash['validCount'] : nil
      invalid_count = hash.key?('invalidCount') ? hash['invalidCount'] : nil
      type = hash.key?('type') ? hash['type'] : nil
      status = hash.key?('status') ? hash['status'] : nil

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      InvitationJobBase.new(token: token,
                            portal_id: portal_id,
                            filename: filename,
                            created_at: created_at,
                            total_count: total_count,
                            valid_count: valid_count,
                            invalid_count: invalid_count,
                            type: type,
                            status: status,
                            additional_properties: additional_properties)
    end

    def to_custom_created_at
      DateTimeHelper.to_rfc3339(created_at)
    end

    # Validates an instance of the object from a given value.
    # @param [InvitationJobBase | Hash] The value against the validation is performed.
    def self.validate(value)
      if value.instance_of? self
        return (
          APIHelper.valid_type?(value.token,
                                ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.portal_id,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.filename,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.created_at,
                                  ->(val) { val.instance_of? DateTime }) and
            APIHelper.valid_type?(value.total_count,
                                  ->(val) { val.instance_of? Integer }) and
            APIHelper.valid_type?(value.valid_count,
                                  ->(val) { val.instance_of? Integer }) and
            APIHelper.valid_type?(value.invalid_count,
                                  ->(val) { val.instance_of? Integer }) and
            APIHelper.valid_type?(value.type,
                                  ->(val) { val.instance_of? String }) and
            APIHelper.valid_type?(value.status,
                                  ->(val) { val.instance_of? String })
        )
      end

      return false unless value.instance_of? Hash

      (
        APIHelper.valid_type?(value['token'],
                              ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['portalId'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['filename'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['createdAt'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['totalCount'],
                                ->(val) { val.instance_of? Integer }) and
          APIHelper.valid_type?(value['validCount'],
                                ->(val) { val.instance_of? Integer }) and
          APIHelper.valid_type?(value['invalidCount'],
                                ->(val) { val.instance_of? Integer }) and
          APIHelper.valid_type?(value['type'],
                                ->(val) { val.instance_of? String }) and
          APIHelper.valid_type?(value['status'],
                                ->(val) { val.instance_of? String })
      )
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token}, portal_id: #{@portal_id}, filename: #{@filename},"\
      " created_at: #{@created_at}, total_count: #{@total_count}, valid_count: #{@valid_count},"\
      " invalid_count: #{@invalid_count}, type: #{@type}, status: #{@status},"\
      " additional_properties: #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token.inspect}, portal_id: #{@portal_id.inspect}, filename:"\
      " #{@filename.inspect}, created_at: #{@created_at.inspect}, total_count:"\
      " #{@total_count.inspect}, valid_count: #{@valid_count.inspect}, invalid_count:"\
      " #{@invalid_count.inspect}, type: #{@type.inspect}, status: #{@status.inspect},"\
      " additional_properties: #{@additional_properties}>"
    end
  end
end
