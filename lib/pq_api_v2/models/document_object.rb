# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

require 'date'
module PqApiV2
  # A KYC or business-verification [Document](page:resources/documents)
  # submitted by a user, returned with HATEOAS navigation links and the current
  # review status. See [Document Status
  # Types](page:additional-api-information/document-status-types) for the
  # lifecycle and [KYC Enums](page:additional-api-information/kyc-enums) for the
  # document-type catalog.
  class DocumentObject < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Date and time when the resource was created. ISO 8601 format.
    # @return [DateTime]
    attr_accessor :created_at

    # Array of document fields and values
    # @return [Array[DocumentDetails]]
    attr_accessor :fields

    # The name given to a computer file in order to distinguish it from other
    # files
    # @return [String]
    attr_accessor :filename

    # A label used to identify a type of data.  Acts like a file extension on
    # the internet.
    # @return [String]
    attr_accessor :mime_type

    # Document token — prefix `docu-`. See
    # [Tokens](page:concepts/working-with-tokens).
    # @return [String]
    attr_accessor :token

    # A map of navigation links related to the current resource, classified by
    # their relationship to the current resource following RFC 8288 / HAL best
    # practices.
    # @return [Hash[String, NavigationLink]]
    attr_accessor :links

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['created_at'] = 'createdAt'
      @_hash['fields'] = 'fields'
      @_hash['filename'] = 'filename'
      @_hash['mime_type'] = 'mimeType'
      @_hash['token'] = 'token'
      @_hash['links'] = '_links'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      []
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(created_at:, fields:, filename:, mime_type:,
                   token: 'docu-2053aaad-c1a5-45e2-a2da-f71287f32800', links:,
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @created_at = created_at
      @fields = fields
      @filename = filename
      @mime_type = mime_type
      @token = token
      @links = links
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      created_at = if hash.key?('createdAt')
                     (DateTimeHelper.from_rfc3339(hash['createdAt']) if hash['createdAt'])
                   end
      # Parameter is an array, so we need to iterate through it
      fields = nil
      unless hash['fields'].nil?
        fields = []
        hash['fields'].each do |structure|
          fields << (DocumentDetails.from_hash(structure) if structure)
        end
      end

      fields = nil unless hash.key?('fields')
      filename = hash.key?('filename') ? hash['filename'] : nil
      mime_type = hash.key?('mimeType') ? hash['mimeType'] : nil
      token = hash['token'] ||= 'docu-2053aaad-c1a5-45e2-a2da-f71287f32800'
      links = NavigationLink.from_hash(hash['_links']) if hash['_links']

      links = nil unless hash.key?('_links')

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      DocumentObject.new(created_at: created_at,
                         fields: fields,
                         filename: filename,
                         mime_type: mime_type,
                         token: token,
                         links: links,
                         additional_properties: additional_properties)
    end

    def to_custom_created_at
      DateTimeHelper.to_rfc3339(created_at)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} created_at: #{@created_at}, fields: #{@fields}, filename: #{@filename},"\
      " mime_type: #{@mime_type}, token: #{@token}, links: #{@links}, additional_properties:"\
      " #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} created_at: #{@created_at.inspect}, fields: #{@fields.inspect}, filename:"\
      " #{@filename.inspect}, mime_type: #{@mime_type.inspect}, token: #{@token.inspect}, links:"\
      " #{@links.inspect}, additional_properties: #{@additional_properties}>"
    end
  end
end
