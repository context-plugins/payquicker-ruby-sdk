# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module PqApiV2
  # Agreement — a legal document (cardholder agreement, terms and conditions,
  # privacy policy, etc.) that a payee or program must accept as part of
  # onboarding or card issuance. Carries the agreement type, the document URL,
  # and the document contents (base64-encoded) for in-app display or download.
  # Read-only; agreements are configured per program by PayQuicker. See
  # [Agreements](page:resources/agreements).
  class AgreementBase < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Token representing the resource. Format is `<prefix>-<uuid>` where the
    # prefix indicates the resource type. See
    # [Tokens](page:concepts/working-with-tokens).
    # @return [String]
    attr_accessor :token

    # Program agreement content
    # @return [String]
    attr_accessor :content_base64

    # Full path of the URI to the content for the program agreement
    # @return [String]
    attr_accessor :url

    # Kind of legal [Agreement](page:resources/agreements) — for example,
    # `CARD_HOLDER_AGREEMENT` (terms a payee must accept before holding a
    # prepaid card), program terms, privacy policy, etc. The full set of
    # agreement types in effect for a program is configured by PayQuicker at
    # onboarding.
    # @return [String]
    attr_accessor :type

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['token'] = 'token'
      @_hash['content_base64'] = 'contentBase64'
      @_hash['url'] = 'url'
      @_hash['type'] = 'type'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      []
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(token:, content_base64:, url:, type:,
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @token = token
      @content_base64 = content_base64
      @url = url
      @type = type
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      token = hash.key?('token') ? hash['token'] : nil
      content_base64 = hash.key?('contentBase64') ? hash['contentBase64'] : nil
      url = hash.key?('url') ? hash['url'] : nil
      type = hash.key?('type') ? hash['type'] : nil

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      AgreementBase.new(token: token,
                        content_base64: content_base64,
                        url: url,
                        type: type,
                        additional_properties: additional_properties)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token}, content_base64: #{@content_base64}, url: #{@url}, type:"\
      " #{@type}, additional_properties: #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} token: #{@token.inspect}, content_base64: #{@content_base64.inspect}, url:"\
      " #{@url.inspect}, type: #{@type.inspect}, additional_properties:"\
      " #{@additional_properties}>"
    end
  end
end
