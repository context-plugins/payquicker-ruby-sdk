# pq_api_v2
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module PqApiV2
  # Request body for `POST /users/search`. Composes `UserSearchFilterItem`
  # entries under `filters` and `UserSearchSortItem` entries under `sort`, plus
  # paging fields. See [Searching](page:concepts/searching) for the two-step
  # search pattern and [Filtering & Sorting](page:concepts/filtering-sorting)
  # for the comparison and direction semantics.
  class UserSearchRequest < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Filter criteria. Multiple filters are ANDed together.
    # @return [Array[UserSearchFilterItem]]
    attr_accessor :filters

    # Sort criteria. Applied in order. If omitted, the endpoint's default sort
    # is used.
    # @return [Array[UserSearchSortItem]]
    attr_accessor :sort

    # Page number (1-based). Defaults to 1.
    # @return [Integer]
    attr_accessor :page

    # Number of results per page. Defaults to the endpoint's limit.
    # @return [Integer]
    attr_accessor :page_size

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['filters'] = 'filters'
      @_hash['sort'] = 'sort'
      @_hash['page'] = 'page'
      @_hash['page_size'] = 'pageSize'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      []
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(filters:, sort:, page:, page_size:,
                   additional_properties: nil)
      # Add additional model properties to the instance
      additional_properties = {} if additional_properties.nil?

      @filters = filters
      @sort = sort
      @page = page
      @page_size = page_size
      @additional_properties = additional_properties
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      filters = nil
      unless hash['filters'].nil?
        filters = []
        hash['filters'].each do |structure|
          filters << (UserSearchFilterItem.from_hash(structure) if structure)
        end
      end

      filters = nil unless hash.key?('filters')
      # Parameter is an array, so we need to iterate through it
      sort = nil
      unless hash['sort'].nil?
        sort = []
        hash['sort'].each do |structure|
          sort << (UserSearchSortItem.from_hash(structure) if structure)
        end
      end

      sort = nil unless hash.key?('sort')
      page = hash.key?('page') ? hash['page'] : nil
      page_size = hash.key?('pageSize') ? hash['pageSize'] : nil

      # Create a new hash for additional properties, removing known properties.
      new_hash = hash.reject { |k, _| names.value?(k) }

      additional_properties = APIHelper.get_additional_properties(
        new_hash, proc { |value| value }
      )

      # Create object from extracted values.
      UserSearchRequest.new(filters: filters,
                            sort: sort,
                            page: page,
                            page_size: page_size,
                            additional_properties: additional_properties)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} filters: #{@filters}, sort: #{@sort}, page: #{@page}, page_size:"\
      " #{@page_size}, additional_properties: #{@additional_properties}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} filters: #{@filters.inspect}, sort: #{@sort.inspect}, page:"\
      " #{@page.inspect}, page_size: #{@page_size.inspect}, additional_properties:"\
      " #{@additional_properties}>"
    end
  end
end
